1. Fire container stacks: docker-compose up -d
2. Build local virtual env: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
3. Boot runtime orchestrator daemon core: python3 main.py
